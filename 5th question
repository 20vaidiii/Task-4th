#Sum of Multiples of 3 or 5 Below N
#Find the sum of all multiples of 3 or 5 below a given number `n`.
#Input Format
#Input: A positive integer n.
#Output Format
#Output: The sum of all multiples of 3 or 5 below n.


def sum_of_multiples(n):
    total_sum = 0
    for i in range(1, n):
        if i % 3 == 0 or i % 5 == 0:
            total_sum += i
    return total_sum

# Input: a positive integer n
n = int(input("Enter a number: "))
# Output: sum of multiples of 3 or 5 below n
print(sum_of_multiples(n))